# 윌로그 HR 대시보드 — Claude Code 작업 컨텍스트

> 최종 갱신: 2026-08-03 (**6-C 발령 이력 정렬** — HrCard_Profile.gs `hp_sortApptHistory()`. **clasp push + 새 버전 배포 + git push 완료**. 2차 정렬 실데이터 검증만 6-3 이후로 보류) · 최초 작성: claude.ai 세션 인수인계
> 이 문서는 저장소 루트에 CLAUDE.md로 두고, 상세 운영은 HR대시보드_운영가이드.md(v3.8)를 함께 참조.
> **다음 작업: 6-3 퇴직자 인사카드 아카이브 (A안 확정, 미착수)**

## 0. ★ 작업 규칙 — 사전 확인 (최우선, 다른 지침에 우선함)

**아래 행위는 실행 전에 반드시 "무엇이 / 어떻게 바뀌는지" 요약하고 Ted의 확인을 받는다.**

| 구분 | 해당 행위 |
|---|---|
| 배포 | `clasp push`, Apps Script 새 버전 배포, `git push`, GitHub Pages 반영 |
| 시트 쓰기 | 스프레드시트 셀·행 기록/수정 (`setValue`/`setValues`/`appendRow` 등)을 **실제로 실행**하는 것. 배치 함수의 실행 자체 포함 |
| 트리거 조작 | 트리거 생성·삭제·시각 변경 (`createDailyTrigger`, `ScriptApp.newTrigger`/`deleteTrigger`) |
| 삭제 계열 | 파일·시트·탭·행·트리거 삭제, `git reset --hard`, 되돌리기 어려운 덮어쓰기 |
| 외부 발송 | Slack 메시지, 메일, 웹훅 호출 등 조직 외부·타인에게 나가는 모든 것 |

확인 요청 시 포함할 것:
1. **대상** — 어떤 파일/시트/탭/트리거인지
2. **변경 내용** — 몇 건, 어떤 값이 무엇으로. 건수가 많으면 유형별 요약 + 대표 예시
3. **되돌리는 방법** — 백업 필요 여부, 롤백 절차
4. 가능하면 **dry-run 결과를 먼저** 제시 (이 저장소의 배치 함수는 대부분 dry-run 모드를 갖고 있다)

**확인 없이 진행해도 되는 것**: 읽기·조회·분석, 코드/문서 편집(로컬 파일), 로컬 `git commit`, dry-run 실행, 오프라인 검증 스크립트.

> 이 규칙은 "요청했으니 당연히 승인된 것"으로 해석하지 않는다. Ted가 "push 해줘"처럼 **명시적으로 그 행위를 지시한 경우에만** 별도 확인 없이 실행한다.

## 1. 시스템 개요

```
[사용자] → willogted.github.io/willog-hr-dashboard (GitHub Pages, index.html 단일 파일)
   → Google Apps Script Web App (URL 불변: AKfycbxvhVUc-ybA8P717Y5skVv9Ewgr8MfhZ6LsSJXTcqYOXUElysN_QM2IrC19SNzdFy0u)
   → Google Sheets "윌로그 HR 대시보드 raw 데이터 입력판_2026" (ID: 196QxIjHyD2KhQ7Q-hvCrtRAKRKZc6UZypGcE9EtyNwg)
```

- **인사발령 공고 원천 시트** (별도 파일): ID `1-pZ0qKTddrINBjWKfrs9kRytcMunr1xOq3vnJu_aPy4` — Louis/Ted가 공고문 작성, 이미지로 내보내 Slack #인사발령(C08HL5SQ54G)에 게시
- Apps Script 파일 6개: Code.gs(웹앱 API, v3.7) / HrCard_Setup.gs(시트 생성+자동화 배치, v2.6) / HrCard_API.gs / HrCard_Profile.gs / SyncAppointments.gs(인사발령 자동 동기화, v1.1) / **SnapshotOrgChart.gs(조직도 월간 스냅샷, v1.1)**
- **조직도 스냅샷 아카이브** (별도 파일, 최초 실행 시 자동 생성): "윌로그 조직도 스냅샷 아카이브" — 파일 ID는 Script Properties `SNAP_ARCHIVE_SS_ID`. 월별 [조직도_YYYY.MM.01] 탭 + 같은 폴더에 PDF (6-B 참조)
- **★ 스냅샷 저장 위치**: 공유 드라이브 폴더. 폴더 ID는 코드가 아니라 **Script Properties `SNAPSHOT_FOLDER_ID`** 에만 있다 → 폴더 변경 시 코드 수정 불필요. **Drive 고급 서비스(Drive API v3) 활성 필요** (appsscript.json)
- 단위: [설정] 입력 만원 → 화면 천원 / 인사카드 연봉 천원
  - **★ 예외: [설정] `ai_본부명` (AI 비용)만 천원 단위 입력** — 2026.07.29 확인. 프론트 `aiCostFor()`가 천원→만원으로 변환해 호출부를 만원으로 통일한다 (3장 10번 참조)
- **★ "새 버전 배포" 판단 기준 = 라우트 변경 여부가 아니라 "웹앱 요청 경로에서 실행되는 코드인가"** (2026.08.03 정정)
  - 트리거용 배치 함수 → 항상 최신 저장 코드(HEAD)로 실행되므로 `clasp push`만으로 반영
  - `doGet`이 호출하는 코드(`readOrgStructured`, `getProfileData` …) → **배포된 버전**으로 실행되므로, 라우트를 안 건드리고 내부 로직만 고쳐도 **새 버전 배포 필요**
  - 이전 표현("라우트 변경 시에만 배포 필요")은 그동안 웹앱 백엔드 로직만 바꾼 사례가 없어서 드러나지 않았던 오해다. 상세 표는 운영가이드 4-4

## 2. 현재 자동화 (매일 트리거 4건 — 2026.07.28 등록 완료)

| 시각 | 함수 | 역할 |
|---|---|---|
| 08시 | autoConvertProbation | 고용형태='수습' + 수습종료일 경과 + 수습평가 최종결과 '통과' → '정규직' 자동 전환. 미통과 경과자는 ⏸ 보류 로그 |
| 09시 | **syncAppointments** | ★ 2026.07.28 신규. 공고 원천 시트 파싱 + 입사/퇴사 발령 자동 생성 → [인사카드_발령] append (5·6장 참조) |
| 09시 | refreshHrCardEvents | [인사카드_이벤트알림] 재생성. 8종: 생일/결혼기념일/연봉만료/비자만료/출산예정/근로계약종료/퇴사예정(최종출근일 기준)/입사N주년(3·5·7·10 🏆) |
| 09시 | refreshHeadcount | [인건비] 월초/월말/입사/자발/비자발 자동 집계 (당월+전월만, 조직도+퇴직자 통합 명부) |

> 2026.07.28 `createDailyTrigger()` 재실행으로 **4건 등록 완료**. (기존 3건은 자동 삭제 후 재등록되므로 중복 없음 — 트리거 함수를 추가할 때마다 이 함수를 1회 재실행하면 된다.)

### 2-1. 월 단위 트리거 (2026.08.03 — **등록 완료**)

| 시각 | 함수 | 역할 |
|---|---|---|
| 매월 1일 07시 | **snapshotOrgChart** | ★ 조직도 월간 스냅샷 → 아카이브 스프레드시트에 [조직도_YYYY.MM.01] 탭 + PDF (6-B 참조) |

- 등록 함수는 `createSnapshotTrigger()` — **`createDailyTrigger()`와 별개**다. 07시로 잡은 이유는 일일 트리거(08·09시)보다 앞이라 실행 창이 겹치지 않게 하기 위함
- **양방향 무충돌 보장**: `createDailyTrigger()`는 자기 4개 핸들러만 삭제하고, `createSnapshotTrigger()`는 `snapshotOrgChart` 핸들러만 삭제한다. 어느 쪽을 재실행해도 상대 트리거는 살아남는다 (오프라인 검증 완료)
- 전체 트리거 목록 확인: `listAllTriggers()` — 등록 후 총 5건(일일 4 + 월간 1)이어야 정상

## 3. 코드베이스 주의사항 (실전에서 확인된 함정)

1. **[인건비] 1행은 배너 텍스트**("...연도+월 입력...") — 헤더 탐지는 반드시 `연도`·`월` 셀 **정확일치**로 (부분일치 시 배너를 헤더로 오인 → 과거 refreshHeadcount가 중복 행 생성했던 버그)
2. **saveCostRow는 1~11열 위치 기반 쓰기** — [인건비] 신규 컬럼은 반드시 12열 이후(맨 끝). 현재 12열 = 특이사항
3. **날짜 셀은 Date 객체로 올 수 있음** — 모든 파서에 toDateStr/parseDate 정규화 필수 ("2026. 5. 17", "2026-05-17", Date 모두 수용)
4. 시트 헤더는 1~3행 내 자동 탐지 + 키워드 매칭이 표준 패턴 (컬럼 순서 무관, 헤더명 변경 금지)
5. 트리거 배치 함수에서 `SpreadsheetApp.getUi()`는 try/catch 필수 (컨텍스트 없으면 예외)
6. 재직 판정: 재직상태에 '퇴' 포함 && '예정' 미포함 = 퇴직. **'퇴직예정'은 재직 중**으로 취급
7. 퇴직일 = 마지막 재직일 (당일까지 재직 카운트)
8. index.html 커밋 검증: GitHub에서 파일 열어 최신 마커 문자열 검색 (다운로드 폴더의 구버전 커밋 사고 이력 있음)
9. **★ append 전용 원장 시트는 행 순서가 정렬 순서가 아니다** — [인사카드_발령]은 수기 정본(시간순)과 `syncAppointments` 백필분(공고 탭 순서)이 섞여 있어 **사람마다 순서가 다르다**. 화면에 시간순으로 보여야 하는 데이터는 **응답 조립 시점에 정렬**한다. 시트를 재정렬해서 해결하려 하면 안 된다 — `syncAppointments`의 행 기반 중복 판정(멱등성)이 깨진다 (6-C)
   - `normalizeDateStr()`이 만든 `"YYYY.MM.DD"` 문자열을 `localeCompare`로 비교하는 방식([인사카드_연봉]·[평가이력]이 쓰는 방식)을 그대로 복사하면 안 된다. V8 `Date`가 못 읽는 값(`"2026년 5월 17일"`, `"미정"`)은 **원문 그대로 통과**하므로 조용히 섞인다 → 발령은 `hp_apptDateKey()`로 별도 파싱한다
10. **★ [설정] `ai_` 키는 천원, `budget_`·`월비용_` 키는 만원** — 단위가 섞여 있다. `aiCostFor()`가 천원→만원 변환을 담당하므로 **호출부는 전부 만원으로 가정해도 된다**. 새 비용 항목을 프론트에 추가할 때 이 함수를 거치지 않고 `S.deptAiCost`를 직접 읽으면 10배 오류가 재발한다
   - 실제 사고: v3.1~v3.8 초기까지 본부별 AI 비용이 10배 과대 표시 + 본부별 '합계' 라인이 인건비(만원)+AI(천원)를 섞어 더하고 있었음 (2026.07.29 수정)
   - 단위 검증 감각: AI 비용은 1인당 월 3~20만원대가 정상. 100만원대가 나오면 단위 오류로 의심할 것

## 4. 시트 스키마 요점 (자동화 관련)

- **[조직도]**: 사번, 닉네임, 한국이름, 재직상태(재직/퇴직예정/퇴직), 본부, 부서, 팀, 직책, 레벨, 입사일, 월고정총액, 연봉총액, status, 퇴사예정일(구 asana 열), 최종출근일(맨 끝)
- **[퇴직자]**: 사번, 닉네임, 성명, 본부, 팀, 직책, 입사일, 퇴직일, 퇴직구분(자발/비자발), 퇴사사유, 상세사유, 퇴직평가, 재입사가능여부, 비고 — 전 연도 누적
- **[인사카드_발령]**: 사번, 닉네임, 발령유형, 발령일, 변경전_소속, 변경전_직책, 변경후_소속, 변경후_직책, 비고 (1행 헤더, 2행 설명, 3행부터 데이터)
- **[인사카드_기본추가정보]**: 사번, 닉네임(영문), ..., 고용형태(수습/정규직/계약직/인턴), 수습종료일, 근로계약종료일(맨 끝) — 현재 재직자·퇴사자 혼재
- 발령유형 어휘: 입사/부서이동/직책변경/직책승격/레벨업/겸직/직무변경/정규직 전환/퇴사 (+조합형 "부서이동/직책승격")

## 5. 공고 원천 시트 파싱 규칙 (2026.07.28 세션에서 확립 — 102건 추출 검증 완료)

- 개인 발령 행 식별: 행 셀 중 **9자리 숫자 사번** 존재 → `사번 | 성명(닉네임) | 발령사항 | 변경 전 | 변경 후 | 발령일자 | 비고`
- 공고 컨텍스트: "인사 공고 XX호" 행과 "공고일자" 행을 순차 추적 (한 파일에 공고 20여 건 연속 배치, **일부 공고는 2벌 중복 존재** → dedup key: 사번+발령일+발령사항+변경후)
- 성명 파싱: "이윤철 (Charles)" / "곽인욱(Roni)" → 닉네임 추출
- 소속/직책 분리: 끝의 (팀장|팀원|부서장|본부장|CEO|COO|Head of X...) → 직책, 나머지 → 소속. "Backend 팀장"처럼 팀명 뒤 '팀' 생략 시 보정. " - " → " " 정규화
- 발령유형 도출: 발령사항 매핑(Job-Level UP→레벨업, 소속 변경→부서이동 등) + '인사 발령'은 전/후 비교 (팀원→팀장=직책승격)
- 특수: 레벨업은 전/후 공란(공고에 수치 없음) / 징계해고 행은 컬럼 밀림 → 소속 비우고 비고 요약
- ~~참고 구현: claude.ai 세션의 parse.py~~ → **SyncAppointments.gs로 이식 완료 (2026.07.28)**

### 5-1. 이식 과정에서 추가로 확인된 함정 (★ 중요)

1. **양식·데모 탭이 섞여 있다** — "인사 공고 25-1호" 탭이 2개인데 그중 하나는 가상 데이터 양식(부서 "New/Super 신설", "(해체)" 등). 이 탭 하나에서 **류휘승(Louis, 250010005) 1명이 같은 날 부서이동·정규직 전환·입사·퇴사 4건**을 받는 구성 → 그대로 반영하면 **재직자에게 가짜 퇴사 발령**이 생긴다.
   - 방어 3중: ① `SA_SKIP_TAB_RE` 탭 이름 패턴 제외 ② **동일 사번·동일 발령일에 발령유형 3종 이상이면 탭 전체 격리** (`sa_looksLikeTemplate`) ③ 입·퇴사 명부 교차검증
   - `listGonggoTabs()` 로 탭 목록·공고번호·사번행수를 찍어보고 필요 시 SA_SKIP_TAB_RE 조정
2. **징계 공고 탭의 더미 사번 `200000000`(성명 `OOO`)이 9자리 규칙에 걸린다** → 사번이 [조직도]/[퇴직자] 명부에 있을 때만 반영하는 로직으로 차단 (다른 탭은 10자리 `2000000000`이라 애초에 미매칭)
3. **테이블 헤더가 '발령일자' 대신 '시행일자'인 탭이 있다** → 둘 다 인식 필요
4. **발령일자 셀에 부연이 붙는다** — `2025. 8. 22 (Jake 퇴사일)` → 앞쪽 날짜만 추출
5. **공고번호에 하이픈이 있다** (`26-03호`) → 공고번호 문자열에는 `sa_norm`(하이픈→공백)을 쓰면 안 됨
6. 공고번호 중복 탭(26-03호, 25-7-2호, 25-1호, 징계 25-1호)은 로그에 경고로 남긴다 — 원본 시트 정리 대상

## 6. 작업 현황

> **2026.07.29 마감 상태** — 6-A **완료** (대시보드 프론트, 배포됨). 다음 작업은 **6-3 (A안 확정, 미착수)**.
>
> **2026.07.28 마감 상태** — 6-1 · 6-2 **운영 반영 완료**.
>
> | 항목 | 결과 |
> |---|---|
> | [인사카드_발령] 행수 | 정본 77 → **294** (데이터 행 기준) |
> | 신규 반영 | **218건** |
> | 재실행 | **0건 추가** — 멱등성 실환경 확인 |
> | 유형 정규화 (6-1-a) | 적용 완료 — `팀 이동/팀 개편/팀명 변경` 잔존 0건 |
> | 검수 확정 (6-1-b) | 적용 완료 — normalize 22키·confirm 14키 전부 `#확정` |
> | 트리거 | `createDailyTrigger()` 실행 → **4건 등록** |
> | 정본 삭제 | Ted가 `240010008 / 2025.03.04` 중복행 1건 직접 삭제 (배치가 아닌 수동 정리) |
>
> 행수 정합: **294 = 정본 77 − 삭제 1 + 신규 218**.

### 6-1 / 6-2. syncAppointments() — ✅ 구현·운영 반영 완료 (2026.07.28, SyncAppointments.gs v1.1)

`syncAppointments()` 하나가 아래 3종을 한 배치로 처리하고 [인사카드_발령]에 **append만** 한다 (기존 행 수정·삭제 없음).

| # | 소스 | 생성 발령 |
|---|---|---|
| ① | 공고 원천 시트 전 탭 | 5장 규칙으로 개인 발령 추출 |
| ② | [조직도] + [퇴직자] 입사일 | `입사` (변경후 = 본부/부서/팀 + 직책) |
| ③ | [퇴직자] 퇴직일 | `퇴사` (변경전 = 최종 소속/직책, 비고에 퇴직구분) |

- **★ 중복 판정 2단계 (v1.1)** — [인사카드_발령] 기존 77행은 Ted가 수기 검수한 **정본**이라 파서와 어휘·띄어쓰기가 다르다 (시트 `팀 이동`/`팀 개편`/`팀명 변경` ↔ 파서 `부서이동`, 시트 `직책변경` ↔ 파서 `직책승격`, `Pulse1` ↔ `Pulse 1`, `Streamline Work` ↔ `Streamline Works`). 1차 키만으로는 같은 발령이 중복 통과된다.

  | 단계 | 키 | 동작 |
  |---|---|---|
  | 1차(엄격) | 사번\|발령일\|발령유형\|변경후_소속 (공백·기호·대소문자 무시) | 일치 → 이미 반영된 것으로 보고 조용히 스킵 |
  | 2차(느슨) | **사번\|발령일** | 기존 행과 일치 → **자동 반영하지 않고** '③ 표기 상이 — 수동확인' 목록으로만 올림 |

  - 2차 매칭은 **기존 시트 행에 대해서만** 적용한다. 같은 날 같은 사람에게 복수 발령(겸직 등)이 실제로 있으므로 **배치 내 후보끼리는 1차 키로만 구분**한다
  - 2차에 걸린 건은 자동 반영이 아니라 전부 사람이 판단한다. 판정사유 칸에 **기존 행 번호 + 기존 유형/소속**을 같이 찍어 비교 가능
  - **검수 끝난 건은 기존 행 비고에 `#확정`** 을 넣으면 이후 실행에서 조용히 스킵 (매일 트리거마다 같은 건이 반복 등장하는 것 방지)
  - `입사`·`퇴사`는 여기에 더해 **사번당 1건**으로 제한 (수기 입력분 보호)
  - 기존 행의 발령일을 못 읽으면 2차 인덱스에서 빠져 중복 반영될 수 있으므로 로그에 경고를 남긴다
- **동시 실행 차단**: LockService (트리거 + 수동 실행 충돌 방지)
- **엔트리 포인트**
  - `previewAppointments()` — ★ **[인사카드_발령] 쓰기 없음**. 전체 결과를 **[발령_프리뷰]** 시트에 구분 컬럼과 함께 기록 (실행 로그는 길면 잘리므로 검수는 시트에서). **처음 실행할 땐 반드시 이걸 먼저**
  - `previewAppointmentsLogOnly()` — 프리뷰 시트 없이 로그로만
  - `syncAppointments()` — 실제 반영 (트리거 등록 대상)
  - `syncBalryeong()` — 공고 동기화만 단독
  - `listGonggoTabs()` — 공고 시트 탭 목록·공고번호·사번행수 (제외 탭 판별용)
- **[발령_프리뷰] 시트** (자동 생성, 매 실행 전체 덮어쓰기, 검수 후 삭제 무방)
  - 컬럼: `구분 | 사번 | 닉네임 | 발령유형 | 발령일 | 변경전_소속 | 변경전_직책 | 변경후_소속 | 변경후_직책 | 비고 | 출처탭 | 판정사유`
  - 구분: `① 신규 예정` / `① 신규 예정 (유형 판별 실패)` / `② 중복 스킵(정확일치)` / `③ 표기 상이 — 수동확인` / `④ 사번당 1건 제한 스킵` / `⑤ 파싱 단계 제외` / `⑨ 점검 필요` — 검수 우선순위 순으로 정렬 + 구분별 배경색
  - A1 배너로 소유권을 확인해 **사람이 만든 동명 시트는 덮어쓰지 않는다**
- **설정 상수** (파일 상단): `SA_GONGGO_SS_ID` / `SA_SKIP_TAB_RE`(제외 탭) / `SA_INCLUDE_LEAVER_HIRE`(기본 true — 퇴직자에게도 입사 발령 소급 생성. false면 조직도 재직·퇴직예정만)
- **발령유형 어휘 확장**: 기존 목록 + `겸직해제`(공고 전/후의 `(겸)` 개수 감소), `징계`, `복직`, `휴직`, 판별 불가 시 `인사발령`(로그 확인용)
- 오프라인 검증(2026.07.28): 공고 27탭 → 후보 107행 중 양식탭 5건·더미사번 1건 제외, **102건 파싱 → 중복 6건 스킵 → 96건 신규**. 2회차 실행 시 added=0 (멱등성 확인)
- ~~선행 1회 작업: xlsx 102건 붙여넣기~~ → **불필요**. `syncAppointments()`가 과거 이력까지 전부 백필한다

### 6-1-a. 정본 유형 표기 정규화 — normalizeAppointmentTypes() — ✅ 적용 완료 (2026.07.28)

[인사카드_발령] 기존 정본 77행의 유형 어휘를 파서 어휘에 맞춰 일괄 정리하는 **1회성 유지보수 함수**. 발령유형·비고 **두 셀만** 쓰고 행 삭제·추가는 없다.

| 규칙 | 내용 |
|---|---|
| ①a | `팀 이동` / `팀 개편` / `팀명 변경` → `부서이동`. **복합유형은 구성요소 단위로 치환 후 중복 제거** (`부서이동/팀 개편` → `부서이동`) |
| ①b | `직책변경` + 변경전_직책 `팀원` + 변경후_직책 `팀장` → `직책승격` |
| ② | 수정 행 비고 앞에 `[원표기: 원래유형]`, 끝에 `#확정` |
| ③ | `250010047 / 2026.01.26` (전략 고문) — 유형 유지, `#확정`만 |
| ④ | `240010004`·`210010008` / `2026.03.16` (26-03호 TFT) — 유형을 `겸직`으로 |
| + | 사번·발령일·정규화유형이 같은 선행 행이 있으면 비고에 `중복의심(N행과 동일)` 표시 (삭제 안 함) |

- 실행: `normalizeAppointmentTypes()` (dry-run) → `normalizeAppointmentTypesApply()` (반영)
- 비고에 `#확정`이 있는 행은 건너뛰므로 **재실행해도 중복 적용되지 않음**(멱등)
- 검증(오프라인, 실제 77행): 변경 23행 / 쓰기 45셀 / 재실행 changed=0 / 발령유형·비고 외 컬럼 무변경 / 행수 무변화
- ~~알려진 잔여 이슈: 10·11행이 동일 발령(240010008 Ted 2025.03.04 직책승격)~~ → **2026.07.28 Ted가 중복행 1건 직접 삭제하여 해소**. 배치가 아닌 수동 정리였으므로, 이후 행 번호를 인용한 기록은 모두 무효 (사번+발령일로만 참조할 것)
- 규칙에 안 걸려 유지한 `직책변경` 5행: 14 Jay(팀장→사업부장) / 16 Emma(직책 공란) / 57 Felix(부서장→공란) / 58 Jenny(팀장→팀원, 강등) / 63 Sean(부서장→부서장)

### 6-1-b. 검수 완료분 일괄 #확정 — confirmReviewedAppointments() — ✅ 적용 완료 (2026.07.28)

6-1-a 정규화 후 [발령_프리뷰]에 남은 `③ 표기 상이` **15건(기존 14행)** 을 Ted가 검수 → **전부 기존 행이 정본**으로 확정. 해당 행 비고에 `#확정`을 붙여 이후 알림에서 제외한다.

- 대상 14행(사번|발령일): `210010008|2025.04.08`, `230010011|2025.04.08`, `240010001|2025.04.08`, `250010028|2025.04.28`(★유형 변경), `230010024|2025.07.14`, `250010001|2025.08.18`, `240010030|2025.11.24`, `250010003|2025.11.24`, `250010007|2025.11.24`, `250010035|2025.11.24`, `250010041|2025.11.24`, `250010042|2025.11.24`, `250010055|2026.05.18`, `250010041|2026.06.08`
- ★ `250010028 / 2025.04.28` (Joy·공고 성명 함현재) 만 `부서이동` → `겸직` + 비고 앞 `[원표기: 부서이동]`
- 15건 vs 14행 차이: `230010024|2025.07.14` 는 공고 후보 2건이 같은 기존 행에 매칭됨
- 실행: `confirmReviewedAppointments()` (dry-run) → `confirmReviewedAppointmentsApply()`
- 검증(실제 데이터, 6-1-a 적용 후 연속 실행): 매칭 14/14 (missing 0) / 쓰기 15셀 / 재실행 changed=0 / 발령유형·비고 외 무변경 / 행수 무변화

> ⚠️ **행 번호로 대상을 지정하지 말 것.** [인사카드_발령]은 1행 헤더·**2행 설명행**·3행부터 데이터라, 설명행을 세지 않으면 행 번호가 1 어긋난다 (실제로 이 차이로 지정 번호와 시트 행이 전부 +1 차이 났음). 유지보수 함수는 모두 **사번 + 발령일**로 매칭한다.
>
> 미해결: 18행 `250010028` 의 [인사카드_발령] 닉네임은 `Joy` 인데 공고 성명은 `함현재`. 어느 쪽이 맞는지 확인 필요 (이번 작업 범위 밖).

### 6-A. 대시보드 프론트 — 전사 요약 스트립 + AI 비용 단위 수정 (v3.8) — ✅ 완료·배포 (2026.07.29)

> 인사발령(6-1~6-3) 트랙과 무관한 **프론트 전용** 작업이라 알파벳으로 구분. 백엔드·시트 무변경이므로 **Apps Script 새 버전 배포 불필요**, `git push`만으로 반영됨.

**① 조직도 탭 최상단 전사 요약 스트립** (커밋 `0a9030d`)
- 4칸: 총 재직 인원 / 전사 인건비 합계(월) / 전사 AI 비용 합계(월) / 기준일
- **새 집계 로직을 만들지 않았다** — `orgWideTotals()`가 본부 박스와 동일한 `empSal()`·`aiCostFor()`를 그대로 합산. 조직별 합 = 전사 값이 구조적으로 보장됨
- 대상 인원 = 퇴직 제외(퇴직예정 포함) — 기존 KPI·탭 배지와 동일 기준
- **필터 무관 항상 전사 기준**, 만원 단위 반올림, PDF 출력 시 `org-noprint`로 제외
- 기준일 = `loadData()` 성공 시각(`S.loadedAt`). payload에 갱신 타임스탬프가 없어서 헤더 연결 배지와 같은 소스를 씀 — **백엔드에 타임스탬프를 추가하면 여기로 갈아끼울 수 있다**

**② ★ AI 비용 단위 버그 수정** (커밋 `5aace9b`) — 3장 10번 함정 참조
- [설정] `ai_` 키는 **천원**인데 프론트가 만원으로 취급 → v3.1 이후 **본부별 AI 비용이 10배 과대 표시** + 본부별 '합계' 라인이 인건비(만원)+AI(천원)를 섞어 더하고 있었음
- `aiCostFor()` 한 곳에서 천원→만원 변환 (`toMan()`), 호출부 2곳(renderOrg `aiCostMan` / orgWideTotals `aiMan`) 동시 교정
- 판정 근거: 최소단위 37.5 = Claude seat 1개 37,500원/월 (Pulse 6명=225 / Heart 5명=187.5 / 각 법인 1명=37.5 정확히 재현) · 전사 합 8,025천원 = 802.5만원 = 실제 청구액 · 만원 해석 시 1인당 $726/월로 비현실적
- 결과: 전사 AI 8,025만원 → **803만원**, Streamline Works 35,625천원 → 3,563천원
- 검증: 라이브 payload를 주입해 실제 렌더 후 본부별 15개 박스 값을 DOM에서 긁어 대조 — 합계 라인 15개 전부 산술 일치, 인원·인건비 무변경
- **잔여 특성(버그 아님)**: 본부별로 천원 반올림되므로 표시값 합(803.1만원)이 스트립(803만원)과 0.1만원 차이날 수 있음. 만원 표기에서는 동일

### 6-B. 조직도 월간 스냅샷 자동화 (SnapshotOrgChart.gs v1.1) — ✅ **구현·운영 반영 완료** (2026.08.03)

> 인사발령(6-1~6-3) 트랙과 무관한 별도 트랙이라 알파벳으로 구분 (6-A와 동일 규칙).
> 백엔드 배치이므로 **웹앱 새 버전 배포 불필요** — `clasp push`만으로 반영.
> 단 PDF 저장·폴더 이동 때문에 **Drive·외부요청 권한이 추가**되고 v1.1에서 **Drive 고급 서비스**가 켜지므로, 최초 수동 실행 시 **권한 재승인**이 필요하다.

매월 1일 시점의 재직자 명단을 전용 아카이브 스프레드시트에 월별 탭으로 쌓고, 같은 폴더에 PDF로 함께 보관한다.

**★ 저장 위치 — 공유 드라이브 폴더 (v1.1, 2026.08.03)**
- 폴더 ID는 **코드에 하드코딩하지 않는다**. 런타임은 **Script Properties `SNAPSHOT_FOLDER_ID`** 에서만 읽으므로, 폴더를 바꿀 때 코드 수정 없이 [프로젝트 설정 → 스크립트 속성] 값만 고치면 된다
  - 폴더 ID는 **코드 어디에도 없다** — 부트스트랩용 `setSnapshotFolderId()`는 설정·마이그레이션 완료 후 삭제했다 (2026.08.03). 유일한 출처는 스크립트 속성
- **공유 드라이브 제약 대응**
  - `SpreadsheetApp.create()` 는 항상 내 드라이브 루트에 만든다 → **생성 직후 이동**한다
  - 이동은 `DriveApp.moveTo()` 대신 **Drive 고급 서비스(Drive API v3)** `files.update` + `addParents`/`removeParents` + `supportsAllDrives:true`. DriveApp은 공유 드라이브 대상에서 실패할 수 있다
  - 공유 드라이브 파일은 **부모가 1개**여야 하므로 `removeParents`에 기존 부모를 반드시 함께 넘긴다 (안 넘기면 이동이 아니라 부모 2개가 되어 실패)
  - PDF도 `folder.createFile()` 대신 `files.create` + `parents` + `supportsAllDrives`
  - 동일 파일명 확인도 `files.list` + `supportsAllDrives`/`includeItemsFromAllDrives`
- **★ 위치는 하드 전제조건** — 아래 상황에서 전부 **로그 남기고 중단**하며, 내 드라이브에 조용히 만들지 않는다 ("잘못된 위치에 쌓이는 게 더 나쁘다")
  - `SNAPSHOT_FOLDER_ID` 미설정 / Drive 고급 서비스 비활성 / 폴더 조회 실패(ID 오타·권한 없음) / 폴더가 아님 / 휴지통 / `canAddChildren` 없음
  - **아카이브 파일이 지정 폴더 밖에 있음** → 기록하지 않고 중단 (자동 이동도 하지 않음 → `migrateSnapshotFilesToFolder()` 안내). v1.0에서 "옮겨도 된다"고 했던 것과 달라진 점
  - 신규 생성 후 이동 실패 시엔 파일 ID를 먼저 저장해 두므로 **재실행해도 중복 생성되지 않는다**
- Drive 쓰기는 **생성·이동뿐** — 파일 삭제·덮어쓰기는 어떤 경로에도 없다

**★ 마이그레이션 검색 범위 (v1.1)**
- 이동 대상은 ① Script Properties의 아카이브 파일(ID로 추적 — 위치 무관) ② **내 드라이브 루트에 있는** `윌로그_조직도_*.pdf` 뿐이다 (`'root' in parents`)
- 드라이브 전체 검색이 아니다 — 수동으로 내보낸 동명 PDF나 다른 폴더에 정리해 둔 PDF를 끌어오지 않기 위해서. 루트가 아닌 곳의 PDF는 **의도적으로 건드리지 않는다**
- dry-run은 파일별 **현재 위치를 폴더명으로** 출력한다 (`snap_folderLabel`) — ID만 보고는 무관한 파일이 섞였는지 판단할 수 없으므로
- 한 번 옮긴 PDF는 루트에 없으므로 다음 실행의 검색 목록에서 아예 빠진다 (재실행 시 `이동 0건`)

**★ 트리거 실행 실패 알림 (v1.1)**
- 월 1회 작업이라 **조용한 실패가 가장 위험**하다 (몇 달 뒤에 발견). 그래서 트리거 실행이 전제조건 실패로 중단될 때는 `snap_abort()`가 **예외를 throw** 해서 Apps Script 실패 알림 메일이 오게 한다
- 컨텍스트 판별: 시간 기반 트리거는 `e.triggerUid`를 넘기고 편집기 수동 실행은 `e`가 `undefined` → `snapshotOrgChart(e)`에서 `fromTrigger`로 전달. **수동 실행은 로그만** (편집기에서 바로 보이므로)
- throw 대상: 폴더 전제조건 6종 / 아카이브 열기 실패 / **아카이브가 폴더 밖** / 이동 실패 / [조직도] 없음 / 인원 0명 / 재직자 0명 / 락 획득 실패(그 달 스냅샷 누락이므로) → 접두사 `[조직도 스냅샷 중단]`
- **★ PDF export 실패도 알림 대상** (2026.08.03 변경) — 단 **시트 기록이 끝난 뒤 맨 마지막(⑧ 단계)에 throw** 한다 → 접두사 `[조직도 스냅샷 PDF 실패]`
  - 시트 쓰기는 ⑥에서 `SpreadsheetApp.flush()`로 확정되고 Apps Script 스프레드시트 쓰기에는 트랜잭션이 없으므로, 이후 throw 해도 **탭·명단은 롤백되지 않는다**. `✅ 스냅샷 기록 완료` 로그도 throw 전에 남는다 (검증 완료)
  - 따라서 `snap_abort()`를 쓰면 안 된다 (그건 기록 전 중단용). PDF 실패는 `snap_run` ⑧에서 별도 처리
- throw 하지 **않는** 것: `같은 이름 탭 존재`(정상 멱등) · `PDF 중복 스킵`(동일 파일명 존재 = 이미 있음)
- 메일 가독성을 위해 throw 메시지는 1줄로 압축하고, 전문은 실행 로그에 남긴다

**★ 데이터 원천 — 대시보드와 동일함을 구조적으로 보장**
- 새 파서를 만들지 않고 `Code.gs`의 **`readOrgStructured(ss)`를 그대로 호출**한다. 이 함수가 웹앱 `action=read`의 `emps`를 만드는 바로 그 함수이므로 "스냅샷 명단 = 대시보드 조직도 탭 명단"이 코드 수준에서 보장된다 (6-A에서 `empSal()`·`aiCostFor()`를 재사용한 것과 같은 원칙)
- 재직 판정도 프론트와 동일: **`재직상태 !== '퇴직'` → 퇴직예정 포함**. `index.html`의 `empSt(e)!=='퇴직'`과 같은 결과이며, 3장 6번 규칙과도 일치
- 문서에 없는 재직상태 값(예: `퇴사`)이 나오면 **로그 경고**를 남긴다 — 표기가 바뀌었을 때 조용히 재직으로 세는 것을 막기 위함 (v2.4.1의 미분류 경고와 같은 취지)
- 컬럼: `사번 | 이름 | 닉네임 | 본부 | 부서 | 팀 | 직책 | 레벨 | 입사일 | 재직상태`
  - 급여 컬럼은 **일부러 제외** — PDF로 공유되는 아카이브라서. 필요해지면 `SNAP_HDR` + row 매핑에 추가
  - `재직상태`는 스펙에 없던 추가 컬럼. 퇴직예정자가 명단에 포함되므로 구분 표시가 없으면 오독 위험이 있어 마지막 컬럼으로 넣었다 (퇴직예정 행은 옅은 붉은 배경)
  - `부서`가 공란이면 `본부` 값이 들어온다 — `readOrgStructured`의 기존 동작(`본부부서: 부서 || 본부`)을 그대로 따른 것
- 입사일은 `snap_normDate()`로 `yyyy-MM-dd` 0패딩. `readOrgStructured`는 Date 셀만 패딩하고 문자열 셀은 구분자만 바꿔서(`2026. 5. 17` → `2026-5-17`) 그대로 두면 **문자열 정렬이 어긋난다**(3월이 8월보다 뒤로 밀림)

**★ 쓰기 범위 — 원본 시트 불침범**
- 쓰기는 **아카이브 스프레드시트 한 곳뿐**. [조직도]·[인사카드_발령] 등 raw 데이터 시트는 **읽기만** 한다
- `readOrgStructured`가 내부적으로 `getOrCreate`를 쓰기 때문에, 호출 전에 [조직도] 존재를 직접 확인한다 (없으면 빈 시트를 만들지 않고 중단)

**★ 멱등성**
| 상황 | 동작 |
|---|---|
| 같은 이름 탭이 이미 있음 | 덮어쓰지 않고 **로그 경고 후 종료**. 같은 달 재작성이 필요하면 기존 탭을 직접 이름 변경/삭제 |
| 같은 이름 PDF가 이미 있음 | 만들지 않고 경고만 (**파일 삭제·덮어쓰기 없음**). 시트 기록은 정상 |
| PDF export 실패 | **시트 기록은 유지.** 로그 기록 후, 트리거 실행이면 맨 마지막에 throw (알림) |
| 아카이브 파일을 열 수 없음 (휴지통 등) | 조용히 새로 만들지 **않고** 중단 + 안내. 새로 만들면 과거 스냅샷이 고아가 되므로 |
| 아카이브가 지정 폴더 밖에 있음 | 기록하지 않고 중단 (자동 이동도 안 함) → `migrateSnapshotFilesToFolder()` 안내 |
| 이미 대상 폴더에 있는 파일 | 이동하지 않는다 (마이그레이션 재실행 안전) |
| [조직도] 시트 없음 / 읽은 인원 0명 / 재직자 0명 | 아무것도 쓰지 않고 중단 |
| TEST 탭 | **유일한 예외 — 덮어쓴다** (반복 검수용). 단 A1 배너로 소유권을 확인해 사람이 만든 동명 탭은 보호 |

**엔트리 포인트**
| 함수 | 동작 |
|---|---|
| `checkSnapshotFolder()` | ★ **먼저 실행.** 저장 폴더 진단 — **읽기만** (쓰기·이동·생성 전부 없음). 폴더명·공유 드라이브 여부·`canAddChildren` 권한·실행 계정·아카이브 현재 위치를 로그로 보여준다 |
| `migrateSnapshotFilesToFolder()` | 기존 파일을 지정 폴더로 이동 — **dry-run**. → `migrateSnapshotFilesToFolderApply()` 로 반영. 아카이브는 **새로 만들지 않고 이동**하므로 파일 ID가 유지된다 |
| `snapshotOrgChartTest()` | ★ **최초 검수용.** 실제 월 탭 대신 **[조직도_TEST]** 에만 기록. PDF는 `윌로그_조직도_TEST_YYYYMMDD-HHmm.pdf` (시각 suffix라 재실행해도 파일 충돌·삭제 없음) |
| `snapshotOrgChartPreviewLog()` | **아무것도 쓰지 않고** 재직자 수·본부별 인원만 로그로 확인 (폴더 설정과 무관하게 동작 — 순수 읽기) |
| `snapshotOrgChart()` | 실제 반영 — [조직도_YYYY.MM.01] + `윌로그_조직도_YYYY.MM.01.pdf`. 트리거 등록 대상 |
| `createSnapshotTrigger()` | 매월 1일 07시 트리거 등록 (2-1장) |
| `listAllTriggers()` | 프로젝트 트리거 전체 목록 — 충돌 점검용 |
| `snap_run({test:false, pdf:true, ym:'2026.09'})` | 과거/특정 월 **백필**용. `ym` 형식이 틀리면 중단 |

**탭 레이아웃**: (TEST만 배너 1행) → 제목(기준일) → 요약(재직자 수·퇴직예정 포함 수·본부 수·생성일시) → 본부별 인원 → 빈 행 → 헤더(고정) → 명단
정렬: 본부 > 부서 > 팀 > 직책서열(높은 순) > 입사일(오래된 순). 직책서열은 `SyncAppointments.gs`의 `SA_POS_RANK`를 재사용(없으면 동순위 폴백)

**설정 상수** (파일 상단): `SNAP_PROP_KEY`(아카이브 파일 ID 키) / **`SNAP_FOLDER_PROP`(=`SNAPSHOT_FOLDER_ID`, 저장 폴더 ID 키)** / `SNAP_ARCHIVE_NAME` / `SNAP_TRIGGER_DAY`·`SNAP_TRIGGER_HOUR` / `SNAP_HDR`·`SNAP_COL_W`

**검증 (2026.08.03, 오프라인 stub 하네스 21종 — Drive API v3까지 stub)**
- 저장 위치 13종: 폴더 미설정 시 진단·중단(**파일 생성 0건**) / 진단 함수 쓰기 0건 / 생성 후 공유 드라이브 폴더로 이동 확인 / **`DriveApp`을 Proxy로 막아 코드 경로에서 미사용임을 증명** / `canAddChildren` 없음·ID 오류·폴더 아님·휴지통 4종 중단 / 아카이브가 폴더 밖일 때 기록 0건 / 마이그레이션 dry-run은 실제 이동 0건 → Apply로 이동 + **아카이브 ID 유지·새 파일 생성 0건** / 마이그레이션 재실행 멱등 / PDF가 항상 지정 폴더 / Drive 고급 서비스 비활성 시 중단 / logOnly는 폴더 무관 순수 읽기 / 트리거 양방향 무충돌(5건)
- v1.0 회귀 8종: 퇴직 제외·퇴직예정 포함·빈행 제외·입사일 0패딩·미상 재직상태 경고 / 같은 달 재실행 무변화 / PDF 중복 스킵+시트 유지 / PDF HTTP 500 시 시트 유지 / 아카이브 휴지통 시 재생성 안 함 / 조직도 없을 때 빈 시트 생성 안 함 / ym 형식 오류 차단 / **전체 시나리오 누적 원본 시트 쓰기 0건**
- 라이브 환경 미검증 — 아래 절차 1~3단계가 실환경 확인 지점

**도입 이력 (2026.08.03 완료)** — 유사 배치를 새로 도입할 때 쓰는 표준 절차
1. ✅ `clasp push` (appsscript.json의 Drive 고급 서비스 포함)
2. ✅ 스크립트 속성 `SNAPSHOT_FOLDER_ID` 설정 → `checkSnapshotFolder()` 로 진단 (**읽기만**). `파일 추가 권한 ✅` 확인
3. ✅ `migrateSnapshotFilesToFolder()` (dry-run) → 파일별 현재 위치(폴더명)로 무관한 PDF 섞임 여부 확인 → `...Apply()` 로 v1.0 때 내 드라이브에 만들어진 아카이브 + TEST PDF 이동
4. ✅ `snapshotOrgChartTest()` → 권한 재승인 → [조직도_TEST] 탭·PDF가 공유 드라이브 폴더에 생성됨 확인
5. ✅ `createSnapshotTrigger()` → `listAllTriggers()` **5건 확인 (일일 4 + 월간 1)**
6. ✅ 부트스트랩 함수 `setSnapshotFolderId()` 삭제 + `clasp push` — 코드에 남은 폴더 ID 리터럴이 나중에 실제 설정값과 어긋날 위험을 제거 (`ai_` 단위 오기가 10배 표시 버그 원인이었던 것과 같은 종류). 이제 폴더 ID는 **코드·문서 어디에도 없고** Script Properties가 유일한 출처다
7. 남은 확인 지점: **다음 달 1일 07~08시 첫 자동 실행 로그** / TEST 탭·TEST PDF는 삭제해도 무방

**폴더를 바꿀 때**: 스크립트 속성 `SNAPSHOT_FOLDER_ID` 수정 → `checkSnapshotFolder()` → `migrateSnapshotFilesToFolder()`/`...Apply()`. **코드 수정 불필요.** 아카이브를 옮기고 속성을 안 바꾸면(또는 반대로) 다음 실행이 위치 불일치로 중단되므로 둘을 함께 처리한다

**일상 운영**: 매월 1일 자동 실행되므로 평소엔 할 일 없음.
- **실패는 알림 메일로 자동 통보**된다 (v1.1) — `[조직도 스냅샷 중단]`(기록 자체 실패) / `[조직도 스냅샷 PDF 실패]`(탭은 정상, PDF만 실패)
- 메일이 오지 않는 것: `⚠ 알 수 없는 재직상태` 경고, 같은 이름 탭으로 스킵(정상 멱등), PDF 중복 스킵 → 실행 로그에서 확인

### 6-C. 인사카드 발령 이력 정렬 (HrCard_Profile.gs) — ✅ 구현·배포 완료 (2026.08.03) · **2차 정렬 실데이터 검증만 6-3 이후로 보류**

> 인사발령 데이터 트랙(6-1~6-3)이 아니라 **조회 화면 표시 순서** 문제라 알파벳으로 구분 (6-A·6-B와 동일 규칙).
> **★ 웹앱 요청 경로(`doGet` → `getProfileData`) 코드이므로 `clasp push` + 새 버전 배포가 모두 필요하다** (1장 배포 기준 참조). index.html은 주석만 바뀌어 기능상 배포 불필요하나 함께 push.

**증상**: 인사카드 발령 이력 탭의 정렬이 **인원마다 뒤죽박죽** (Ted 240010008 오름차순 / Louis 250010005 내림차순).

**원인**: `getProfileData`가 [인사카드_발령] **시트 행 순서를 그대로 반환**하고 있었다. 정렬 코드가 아예 없었다.
- 같은 함수의 [인사카드_연봉]·[평가이력]에는 정렬이 있는데 **발령만 빠져 있었다**
- 시트 행 순서가 사람마다 다른 이유: 정본 77행은 수기 입력분이라 시간순, `syncAppointments` 백필분은 공고 탭 순서(배치 **내부에서만** 발령일 오름차순 정렬해 append)라 회차가 갈리거나 입사/퇴사 발령이 나중 회차에 붙으면 개인 단위 순서가 깨진다
- 프론트에는 정렬이 없었으므로 **이중 정렬은 아니었다** (제거할 것 없음)

**해결** — `hp_sortApptHistory()` (신규, `HrCard_Profile.gs`)
| 순위 | 기준 |
|---|---|
| 1 | 발령일 **내림차순** (최신이 맨 위) |
| 2 | 같은 날짜면 **발령유형 서열** 내림차순 |
| 3 | 동순위는 **시트 순서 유지** (안정 정렬) |
| — | 발령일 없음·파싱 불가 → **맨 아래** + `Logger` 경고 |

- 유형 서열 `HP_APPT_RANK` (요구 7종): 입사10 ‹ 부서이동20 ‹ 직책승격30 ‹ 겸직40 ‹ 겸직해제50 ‹ 레벨업60 ‹ 퇴사70 → 내림차순이므로 **화면에서는 퇴사가 위, 입사가 아래**
- `HP_APPT_RANK_EXT` (요구 7종 외 실사용 어휘, 7종 사이에 끼움): 복직12 · 정규직전환15 · 직무변경25 · 직책변경30(직책승격과 동급) · 휴직55 · 징계65. **여기에도 없는 유형은 0** → 같은 날짜 그룹 맨 아래 (`인사발령`= 파서 판별 실패분이 여기 해당)
- 조합형(`부서이동/직책승격`)은 `/ , · + &` 로 쪼개 **구성요소 중 최고 서열**. **토큰 정확일치**로 판정 — 부분일치를 쓰면 `겸직해제`가 `겸직`으로 잡힌다
- 날짜는 `hp_apptDateKey()`로 `"YYYYMMDD"` 정렬키를 만든다. `normalizeDateStr()` 결과를 `localeCompare`하지 않는 이유는 3장 9번 참조. `new Date()` 재파싱도 하지 않는다(로케일·타임존 의존) — 정규식으로 `2026.05.17` / `2026. 5. 17` / `2026-5-17` / `2026/05/17` / `2026년 5월 17일` + 뒤 부연(`2025. 8. 22 (Jake 퇴사일)`)까지 수용하고 월 1~12·일 1~31 범위를 검증
- **★ 시트는 재정렬하지 않는다** — 응답 조립 시점에만 정렬. 시트 행 순서를 바꾸면 `syncAppointments`의 행 기반 중복 판정(멱등성)이 깨진다. 정렬 함수는 시트 API를 전혀 호출하지 않고 입력 배열도 변형하지 않는다(행 객체 참조만 재배열)
- **정렬 지점은 서버 한 곳뿐** — `index.html`은 서버 순서를 그대로 렌더한다. 재발 방지로 렌더 지점에 "여기서 다시 정렬하지 말 것" 주석을 넣었다
- `HrCard_API.gs`의 `getHrCardData()`(=`action=getHrCard`, **현재 프론트 미사용 레거시**)에도 같은 헬퍼를 적용해 순서 규칙을 일치시켰다

**검증 (2026.08.03, 오프라인 단위 테스트 22종 전부 통과)**
- 날짜 정렬 5종: 오름차순 입력→내림차순 / 이미 내림차순→유지 / 뒤죽박죽→내림차순 / 포맷 혼재(Date 객체·0패딩 없음·하이픈·`년월일`·부연 붙음) / 0패딩 없는 3월이 8월보다 위로 오지 않음
- 유형 서열 6종: 요구 7종 전체 순서 / 조합형 최고서열 / **겸직해제 vs 겸직 부분일치 함정** / 미지정 유형 최하위 / 확장 어휘 / 안정 정렬
- 결측 3종: 빈문자·null·undefined·`미정` → 맨 아래 + 경고 로그 1건(사번·건수·원본값 포함) / 파싱 불가 판별(13월·32일·잘못된 Date 포함) / 전부 결측
- 불변·경계 5종: 입력 배열 불변 / 50행 누락·중복 없음 / 빈 배열·null 입력 / **`SpreadsheetApp` 접근 시 throw 스텁으로 시트 미접근 증명** / 정렬 멱등
- 실데이터 유사 3종: 26-03호 TFT 같은 날 겸직+부서이동+레벨업 / 퇴사자 / 같은 날 입사+퇴사
- 테스트가 실제 결함 1건을 잡았다 — `val instanceof Date`가 **다른 실행 컨텍스트에서 온 Date를 놓친다**. 테스트를 느슨하게 하는 대신 `getTime` 유무를 함께 보도록 코드를 고쳤다

**반영 (2026.08.03)**: `clasp push` → Apps Script 새 버전 배포 → `git push` 완료. GitHub 원본 파일에서 `hp_sortApptHistory` 반영 확인.

**라이브 검증 상태**
- ✅ **1순위(발령일 내림차순)** — 재직자 카드에서 최신순 확인
- ⏸ **2순위(같은 날짜 유형 서열) 미검증** — 오늘 확인한 Dawson 케이스는 **시트 순서와 스펙 결과가 우연히 같아 판별력이 없었다**. 유일한 실데이터 판별 케이스인 이회민(240010030)은 **퇴직자라 6-3 이전에는 카드를 열 수 없어** 보류 → 6-3 검증 항목으로 이관
- 즉 유형 서열은 **단위 테스트로만 검증된 상태**다. 2순위 로직을 손볼 때는 이 점을 감안할 것

### 6-3. ★ 다음 작업 — 퇴직자 인사카드 아카이브 (**A안 확정 · 미착수**)

> 2026.07.28 기준 **다음에 착수할 작업**. 설계는 A안으로 확정됐고 구현만 남았다. 작업 지점 3곳:
> 1. `index.html` — 퇴직자 관리 목록 행 클릭 → `openProfile(사번)` 연결 (프론트, GitHub Pages 배포 필요)
> 2. `HrCard_Profile.gs` + `index.html` — 인사카드에 '퇴직 정보' 섹션 추가 ([퇴직자] 시트의 퇴직일/구분/사유/퇴직평가/재입사가능여부)
> 3. 배치 — [인사카드_기본추가정보]에 '재직구분' 컬럼 자동 표기 (조직도 대조). **신규 컬럼은 맨 끝에 추가** (3장 2번 주의사항)
>
> ※ 1·2번은 웹앱 응답 형태가 바뀌므로 **Apps Script 새 버전 배포가 필요**하다 (지금까지의 배치 작업과 다른 점).

Ted 요구: 퇴직자 인사카드(퇴직평가 포함)를 별도로 조회 가능하게 + [인사카드_기본추가정보] 재직/퇴사 혼재 해소.
**권장안 (A)**: 행을 옮기지 **않는다**. 근거 — getProfile은 사번 기반이라 퇴직자도 이미 전체 이력 조회 가능. 대신:
  1. 대시보드 퇴직자 관리 목록에 행 클릭 → openProfile(사번) 연결 (프론트 소폭 수정)
  2. 인사카드에 '퇴직 정보' 섹션 추가: [퇴직자] 시트의 퇴직일/구분/사유/퇴직평가/재입사가능여부 표시 (HrCard_Profile.gs + index.html)
  3. 혼재 해소는 이동 대신 **[인사카드_기본추가정보]에 '재직구분' 컬럼 자동 표기** (배치가 조직도 대조해 재직/퇴직 기입) → 필터로 분리. 데이터 이동이 없어 getProfile·수습·이벤트 로직 전부 무영향
**대안 (B)**: 인사카드Archive_* 탭으로 행 물리 이동 — getProfile이 양쪽 탭을 모두 조회하도록 전 함수 수정 필요, 복잡도·리스크 큼. 비권장.

**★ 함께 처리할 검증 항목 (6-C에서 이관)**
- 퇴직자 카드 열람이 가능해지면 **이회민(240010030)의 2025.12.12 발령 2건**이 화면에서 **퇴사(위) → 겸직해제(아래)** 순서로 나오는지 확인한다
- 이게 **2차 정렬(같은 날짜 유형 서열)의 유일한 실데이터 판별 케이스**다. 오늘(2026.08.03) 확인한 Dawson 케이스는 시트 순서와 스펙 결과가 우연히 같아 판별력이 없었고, 이회민은 퇴직자라 6-3 이전에는 카드를 열 수 없어 검증을 보류했다
- 순서가 반대로 나오면 `HP_APPT_RANK`의 `겸직해제`(50) vs `퇴사`(70) 비교 또는 `hp_apptTypeRank()`의 토큰 분리를 의심할 것 (6-C)

### 6-4. 추가 자동화 제안 (우선순위 논의)
1. **정합성 일일 점검 리포트**: 사번 교차검증(조직도↔기본추가정보↔발령), 필수값 누락(고용형태·수습종료일·생년월일 등), 발령 났는데 조직도 미반영 감지 → 로그 또는 Slack
2. **신규입사자 스캐폴딩**: 조직도에 있는데 인사카드_기본추가정보 행 없는 사번 → 사번/닉네임 채운 빈 행 자동 생성 (입력 누락 방지)
3. **주간 HR 다이제스트**: 임박 이벤트 + 수습 현황 + 입퇴사 요약을 매주 월 09시 Slack Incoming Webhook으로 #boost_hr 발송 (웹훅 URL 1개 필요)
4. 장기: KPI/OKR "성과·목표" 탭 (별도 라우트 action=readPerf) — 원천(Asana 스냅샷 vs API) 및 피드백 민감도 결정 대기 중

### 6-5. 백로그 — 오프라인 테스트 하네스를 저장소에 편입 (**미착수**, 2026.08.03 보류 결정)

지금까지의 검증 하네스(6-B의 stub 21종, 6-C의 단위 테스트 22종 등)는 전부 **스크래치패드에만 있고 저장소에 없다.** 회귀 테스트로 재사용하려면 편입이 필요하나, **`apps-script/` 안에 넣으면 안 된다.**

- `.clasp.json`이 `rootDir: ""` + `skipSubdirectories: false` 이고 **`.claspignore`가 없다** → `apps-script/` 하위에 `.js`를 추가하면 **`clasp push` 대상에 자동으로 같이 잡혀** Apps Script 프로젝트에 테스트 파일이 올라간다 (`clasp status`의 Tracked files 로 확인 가능)
- 따라서 편입 시엔 **저장소 루트 `tests/` 폴더 + `.claspignore` 추가**를 한 묶음으로 처리한다. push 대상이 7개 파일 그대로인지 `clasp status`로 확인하는 것까지 포함
- 그때 함께 정할 것: 실행 방법(`node tests/*.js` 수동 vs npm script), Apps Script 전역(`Logger`·`SpreadsheetApp`) stub 공통화

### 6-6. 백로그 — 대시보드 접근통제 강화 검토 (**미착수**, 우선순위: 6-3 다음)

**현재 구성**: `index.html`의 클라이언트 비밀번호 게이트 + 웹앱 액세스 "모든 사용자(익명)".

| 문제 | 내용 |
|---|---|
| 게이트 코드 노출 | 저장소가 **PUBLIC**이라 `index.html:822`의 `const PW` 리터럴이 그대로 공개된다 (바로 위 821행에 웹앱 URL도 함께) |
| 게이트 우회 | 비밀번호 검사는 **브라우저에서만** 한다(`doLogin()`, index.html:859). 웹앱 엔드포인트를 직접 호출하면 게이트를 거치지 않고 전 직원 인사·급여 데이터가 응답된다 — `appsscript.json`의 `access: "ANYONE_ANONYMOUS"` + `executeAs: "USER_DEPLOYING"` 조합이라 URL만 알면 누구나 조회 가능 |

**근본 대안**: 프론트를 **Apps Script HTML Service로 이전**해 웹앱 액세스를 **Willog 계정으로 제한**한다 (`access`를 도메인 한정으로, 인증은 Google이 담당). 그러면 게이트 코드가 클라이언트에 없어도 되고 엔드포인트 직접 호출도 막힌다.
- 트레이드오프: GitHub Pages 배포 워크플로(`git push`)가 없어지고 `clasp push` + 새 버전 배포로 통합된다 → 프론트 수정 절차가 바뀐다 (운영가이드 4장 개정 필요)
- 중간 단계 대안: 저장소를 private로 전환하고 Pages만 분리 / 웹앱에 토큰 파라미터 요구 — **둘 다 근본 해결은 아니다**(전자는 게이트 우회가 남고, 후자는 토큰이 다시 클라이언트에 노출됨)

**함께 검토할 별건**: CLAUDE.md·운영가이드의 **사번·닉네임·발령 사례 익명화**. 공개 저장소에 개인 식별 정보가 들어 있다 (예: 6-1-b의 사번 목록, 6-3의 이회민 케이스, `250010028` Joy/함현재 불일치 기록). 문서를 저장소에서 분리할지, 사번을 마스킹할지 결정 필요.

## 7. 개발 워크플로 (Claude Code)

> ⚠️ 아래 명령 중 `clasp push` · `git push` · 배포 · 배치 함수 **실제 실행**은 전부 **0장 사전 확인 대상**이다. 코드 수정과 로컬 커밋까지는 확인 없이 진행하고, 반영 직전에 변경 요약을 제시한다.

```bash
# 백엔드 수정 후
clasp push                  # .gs 반영 (트리거 함수는 이것으로 끝)
# 웹앱 라우트(doGet 응답) 변경 시에만: Apps Script UI에서 배포 관리 → 새 버전
# 프론트 수정 후
git add index.html && git commit && git push   # GitHub Pages 1~5분 반영
# 검증: 대시보드 Ctrl+Shift+R, Apps Script 실행 로그, 트리거 메뉴 오류율
```

배치 함수 수동 실행/검증: Apps Script 편집기에서 함수 선택 → 실행 → 실행 로그 확인 (refreshHeadcount는 "갱신"으로 찍혀야 정상, "신규행"이면 헤더 탐지 문제).

### syncAppointments 최초 실행 절차 — ✅ 2026.07.28 완료 (신규 도입 시 재사용)

> 이 절차는 이미 1회 완주했다. 아래는 기록 겸, 유사 배치를 새로 도입할 때 쓰는 표준 절차.

1. `listGonggoTabs()` 실행 → 탭 목록 확인. 양식·연습용 탭이 `⛔` 표시 없이 나오면 그 탭 이름을 `SA_SKIP_TAB_RE`에 추가하고 clasp push
2. `previewAppointments()` 실행 → **[인사카드_발령] 쓰기 없음**. **[발령_프리뷰] 시트**에서 검수 (로그는 요약만)
   - `③ 표기 상이 — 수동확인`: 판정사유의 기존 행 번호와 대조. 정본이 맞으면 그 기존 행 비고에 `#확정` 추가, 별개 발령이면 프리뷰 행을 [인사카드_발령]에 **수동 복사**
   - `① 신규 예정 (유형 판별 실패)`: 발령유형이 `인사발령`으로 찍힌 건 → 유형 직접 지정
   - `⑤ 파싱 단계 제외` / `⑨ 점검 필요`: 빠지면 안 될 발령이 섞였는지 확인
3. [인사카드_발령] 시트를 **사본으로 백업** (append 전용이라 롤백은 추가된 행 삭제로 가능하나, 첫 회는 100건 단위라 백업 권장)
4. `syncAppointments()` 실행 → 신규 건수가 2단계 프리뷰의 `①` 합계와 일치하는지 확인
5. 한 번 더 `syncAppointments()` 실행 → **`① 신규 0건`** 이어야 정상 (멱등성 확인)
6. `createDailyTrigger()` 실행 → 트리거 4건 재등록 (기존 3건 자동 정리)
7. 트리거 메뉴에서 다음 날 09시 실행 결과·오류율 확인

> 운영 중에도 `③ 표기 상이` 건은 절대 자동 반영되지 않는다. 매일 로그의 `③` 카운트만 보고, 0이 아니면 `previewAppointments()`로 [발령_프리뷰]를 열어 판단하면 된다.

### 일상 운영 (2026.07.28 이후)

- 새 인사 공고를 원천 시트에 추가하면 **다음 날 09시 트리거가 자동 반영**한다. 평소엔 할 일 없음
- 확인할 것은 실행 로그의 세 숫자뿐: **`③ 표기 상이`** (0이 아니면 검수), **`⑤ 파싱 단계 제외`**, **`⑨ 점검 필요`**
- `③`이 뜨면: `previewAppointments()` → [발령_프리뷰]에서 판정사유 확인 → 기존 행이 정본이면 그 행 비고에 `#확정` 추가 / 별개 발령이면 프리뷰 행을 [인사카드_발령]에 수동 복사
- **건수 검증은 시트에서 직접 한다.** Google Drive로 내려받은 스프레드시트 텍스트 덤프는 행이 누락될 수 있다 — 2026.07.28에 덤프 파싱으로 274행/198건이라 집계했으나 실제는 **294행/218건**이었다. 대용량 시트 덤프는 근거로 쓰지 말 것
- 미해결 데이터 이슈: `250010028` 의 [인사카드_발령] 닉네임 `Joy` ↔ 공고 성명 `함현재` 불일치 — 어느 쪽이 맞는지 확인 필요
